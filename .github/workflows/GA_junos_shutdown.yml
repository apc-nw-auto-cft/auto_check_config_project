---
name: junos_shutdown
run-name: ${{ github.actor }} is exec junos_shutdown
on: [workflow_dispatch]
jobs:
  # check_my_ip:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: check my ip
  #       run: curl http://inet-ip.info/

  # approval:
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: test
  #   steps:
  #     - run: echo "waiting approval"

  ansible_in_docker:
    runs-on: ubuntu-latest
    # container:
    #   image: ghcr.io/apc-nw-auto-cft/auto_check_config_project/ee:1.2
    #   credentials:
    #     username: ${{ github.actor }}
    #     password: ${{ secrets.ec2 }}
    #   options: --user root
    steps:
      - name: Login to Github
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: "uchida0411"
          password: "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"
      
      - name: check my ip
        run: curl http://inet-ip.info/

      - name: checkout code
        uses: actions/checkout@v3

      # - name: sleep
      #   run: sleep 60

      - name: connectivity_check
        uses: corrupt952/retry-command@v1.0.7
        with:
          command: ansible-playbook -i inventory junos_shutdown.yml --tags "connectivity_check"
          max_attempts: 5
          retry_interval: 5
      
      - name: cat file
        run: cat test.yml

      - name: ansible test
        run: ansible-playbook -i inventory junos_shutdown.yml
